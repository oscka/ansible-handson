version: '3'

services:
  nginx-proxy:
    image: nginx
    ports:
      - '80:80'
      - '443:443'
      - '6443:6443'
    volumes: 
      - {{ component.INSTALL_ROOT }}/docker/nginx-proxy/nginx-proxy-config.conf:/etc/nginx/nginx.conf