version: '3'
services:
  haproxy:
    image: haproxy:alpine
    container_name: haproxy
    restart: always
    ports:
      - '80:80'
      - '443:443'
      - '6443:6443'
    volumes:
      - {{ component.INSTALL_ROOT }}/docker/haproxy/haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg
   